{
	"private": true,
	"name": "@wpelevator/oauth-pilot",
	"scripts": {
		"build": "npm-run-all build:*",
		"build:php": "composer install --no-plugins --no-dev --optimize-autoloader",
		"dev-client": "node tools/dev-client/server.mjs"
	},
	"devDependencies": {
		"express": "^5.2.1",
		"npm-run-all2": "^9.0",
		"oauth4webapi": "^3.8.7"
	}
}
